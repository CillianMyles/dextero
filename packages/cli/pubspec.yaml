name: dextero_cli
description: Terminal client for the local Dextero server.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

resolution: workspace

dependencies:
  dextero_server:
    path: ../server

dev_dependencies:
  dextero_core:
    path: ../core
  lints: ^6.0.0
  test: ^1.31.0
