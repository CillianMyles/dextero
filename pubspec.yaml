name: dextero_workspace
description: Workspace tooling for the Dextero Dart monorepo.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

workspace:
  - packages/core
  - packages/server
  - packages/app
  - packages/cli

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
